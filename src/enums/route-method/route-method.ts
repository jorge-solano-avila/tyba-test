/**
 * Enum with the possibles route methods that the application receive (GET, POST, PUT and DELETE).
 */
export enum RouteMethod {
  Get = "GET",
  Post = "POST",
  Put = "PUT",
  Delete = "DELETE"
}
